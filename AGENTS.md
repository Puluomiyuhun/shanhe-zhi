# 山河志：给接手 agent 的入口

本文件是项目工作约定，用户当次指令优先。它不是完整对话记录，也不替代读代码。

## 开始工作

1. 阅读 [当前上下文](docs/PROJECT_CONTEXT.md)：产品方向、用户偏好、实现边界、待办建议。
2. 阅读 [开发与交接](docs/DEVELOPMENT.md)：代码地图、启动、验证、性能和发布边界。
3. 检查 `git status --short`、最近提交以及本次涉及的源码，再动手。保留用户未提交的修改。
4. 根目录旧设计稿和迭代记录是历史资料；与代码冲突时以当前代码及用户最新要求为准。文档索引见 [docs/README.md](docs/README.md)。

## 重要约定

- 多时代主开发对象为 `dist/engine/` + `dist/scenarios/`，入口 `/game/`；详见 [剧本引擎](docs/SCENARIO_ENGINE.md)。`dist/chunqiu/` 旧脚本仅留对照，勿继续修改。原三国 `dist/lab/` 保留；`dist/index.html` 默认进入它，`dist/classic.html` 才是旧二维版。不要把旧地图恢复成默认入口。
- 这是静态 HTML/CSS/原生 JavaScript + 本地 Three.js 项目。没有 npm 安装或构建的必要；不要仅为了套模板重建框架。
- 左键查情报，右键直接移动并自动推进；保留路线及目标。空格在普通按钮焦点下也必须能暂停／推进。输入区域和弹窗保留键盘语义。
- 主角和 AI 连续移动，模拟在到达格心时结算。不要恢复固定 330 ms 动画加独立格子定时器的实现。
- 所有城市保留远景标记，拥挤时收成小旗，悬停可读；驻城主角仅在城市旗牌标「主」。
- 改 UI 要进行相关浏览器交互／视觉检查；改逻辑运行对应 `verify-*.mjs`。纯文档改动检查链接与事实即可。
- 沟通使用中文，说明完成项、验证结果和真实限制。计划和占位数据不能描述为已实现功能。
- 账号凭据、会话 token、本机私有路径、个人信息不进入代码或笔记；不要复制原始聊天日志。素材出处与许可证要保留。
- 完成影响产品方向、操作、架构或性能的改动后，更新当前上下文或相关开发记录，防止下一次交接读到过时信息。

## 提交和发布

先确认当前 checkout 与 remote；按用户授权提交或推送，不做强制推送。文档提交无需重新部署游戏。普通 GitHub clone 不包含原开发机器的 Sites 私有配置；无法从代码推断部署权限或凭据。详见开发文档。
