# 荆襄三维样区 · 第一轮验证

入口：dist/lab/index.html。原版入口与存档保持独立，样区不自动存档。旧版帮助窗口也有样区链接。

## 已实现

- 真实WebGL2三维场景：连续高度地形、河谷、三座模块化城池、林木实例、田地、渡桥、光照与阴影。
- 同一地图远近缩放，近景展开六角线；可平移、右键旋转、回到部队及全图。
- 1920个独立逻辑地块，地形费用、不可通行河流/险山、六邻接寻路和贴地选中/路线预览。
- 一支先遣部队：确认命令、逐步/自动推进、暂停、取消及敌方地块占领。
- 基于己方可通行地块的补给连通检查。到新野后可模拟后方封锁；失联行军损失士气，解除后恢复连通。
- 原创程序化模型；Three.js 0.180.0打包在站内并附MIT许可证，不依赖运行时外部CDN。

## 已完成的验证

纯逻辑验证：路线相邻合法、必经渡桥、不进入河流/险山、抵达位置、封锁断供、断供士气损失、解除恢复、北上占领后补给连通、重置恢复初始归属。

浏览器操作验证：远近镜头、地面选格、路线预览、确认军令、18步抵达新野、封锁后显示中断、解除后恢复连通；控制台未见错误。渲染统计在当前内置浏览器观察约60FPS、P95约16.8ms、约19.4万三角面、视角不同约80—111次绘制。此为单一浏览器当前场景采样，不是跨设备性能承诺，也未测集显/手机压力场景。

## 本轮范围与后续

这是可玩技术样区，不是正式美术品质版本。山地是手工约束的程序化高度场，城址布局为样区概化；不宣称精确复原。只有玩家一队，封锁是可控测试事件，尚无敌军战斗与全国AI。资源和兵力未做正式模拟。人物成长仍在原版中。

先以原生ES模块实现可验证样段；后续通过样区评审后再进入TypeScript模块化迁移。规模从预研中的约7000格缩小至1920格，先验证规则与镜头，未提前以大地图规模宣称性能达标。

下一轮优先：美术样板（城墙、屋顶、树冠、河岸、山脊细节）；更柔和的格线、连续镜头锚点与触屏双指缩放；分块地形和实例LOD；带实际敌军动作的补给切断；再接回单武将指挥权限。当前格线按镜头自动变淡，但森林实例尚未按距离切换几何LOD。

操作：滚轮缩放、左键拖动平移、右键拖动旋转。点击地面选择地块；右键轻点/Esc取消预览，空格切换推进。刷新或重置重新演练，不写入原版行记。

## 第二轮：美术样板（2026-09-23）

新建 art.js 隔离材质与环境建模。山体改为连续折叠山脊，坡面混合岩色与土色，地表增加多尺度噪声和细微凹凸；外围地形延伸并加入远雾。树木改为五簇不规则树冠与独立树干的实例模型。城池补上双坡瓦顶、抬起的屋檐、城垛、四角楼、门洞、街道和官署院落。田块随地形起伏，采用四种共享材质与垄纹；汉水增加岸石、芦苇和水纹。材质贴图均为确定性程序生成，不使用商业游戏素材。

城池、桥梁、田地按材质合批并保留 UV。势力覆盖和格线细分贴地，降低透明度；格线按距离淡出。保留原有 1920 格、寻路与补给逻辑。

本轮验证：纯逻辑回归全部通过；浏览器实际确认 18 格行军抵达新野，后方封锁中断补给，解除恢复；控制台未见错误。当前内置浏览器远近景观察约 60 FPS、P95 16.8 ms、43 次绘制、148 万三角面。树冠细节提高了面数，尚未做实例 LOD 和低端设备验证，此结果不代表全地图规模性能。

目前依然是程序化美术样板，不能等同《三国志14》的最终资产质量。下一步应优先验证精制城池资产、植被 LOD 与自然地形材质，再扩大区域。

## 第三轮：军令界面与格线（2026-09-23）

参考《三国志14》官方主画面信息分区（https://www.gamecity.ne.jp/manual/JuLnHe14/jp/3300.html），重新设计原创深蓝漆面与铜金描边界面：顶部势力和实际行军里程、右侧部队军令与浅色地形情报卡、底部推进控制、左下军情反馈。保留现有操作，兵站测试折叠收纳，渲染统计移入帮助。实际控制土地数、士气条、军令状态与演练数据同步，不添加未实现的资源数值。

六角线改为更明亮的米金色，近景透明度提高至约 0.22，远景保留约 0.024，继续随缩放自动淡出。浏览器验证远近视图、选择路线、确认／推进／暂停、18 格抵达后的里程、折叠兵站面板、补给断开与恢复、帮助弹窗；控制台未见错误。修复按钮获得焦点时空格键可能同时触发全局推进的问题。
