# 山河志：共用引擎与剧本包

2026-09-24。新入口 `/game/`；春秋旧链接 `/chunqiu/` 自动进入 `/game/?scenario=chunqiu`。三国共用引擎版为 `/game/?scenario=sanguo`。根入口与 `/lab/` 原三国独立版保留，作为迁移对照。

## 目录与边界

- `dist/engine/`：共用渲染、六角地格、寻路、移动、AI 行动、情报和 UI。
- `dist/scenarios/<id>/data.js`：版本化数据，含势力、城池、人物、主角、行动角色、地图尺寸和河湖山道关隘。
- `terrain.js`：纯地形高度提供器；接收噪声、山系、城市辅助数据，不依赖 DOM 或全局游戏状态。
- `manifest.js`：组合数据与地形提供器；`engine/catalog.js` 是受控剧本目录。
- `schema.js`：载入前校验城池、归属、人物、行动目标及地理数据。未知剧本显示错误，不能任意导入路径。
- `runtime.js`：单页只加载一个剧本；切换通过整页导航重建场景，避免旧人物、事件监听、地形缓存混用。

春秋为 28 势力、38 城、146 人物、10 个活动角色；三国为 9 势力、35 城、89 人物、6 个活动角色。三国沿用原区域数据与高度逻辑，但河流转为通用折线水系，不是逐像素一致迁移。春秋旧 JS/CSS 暂留历史对照，当前入口已不再执行；维护应修改 engine/scenarios，回归测试已改为共用引擎。

## 新增时代

1. 在 scenarios 新建目录，参照现有剧本提供 data、terrain、manifest 三个模块。
2. 数据中填好 schemaVersion、id、title、description、player、terms、map、factions、cities、officers、actors；数据为可信项目内容，不接受未审查的外部脚本。
3. 在 catalog 注册唯一 ID 和选择卡信息。地图尺寸与渲染范围必须包住城邑和地理要素。
4. 执行 `node verify-scenarios.mjs`；PowerShell 可设置 `$env:SCENARIO='sanguo'` 选择目标，测试完 Remove-Item Env:SCENARIO。
5. 浏览器检查地图、情报筛选、头像、主角返回出发地和 AI 路线。新增剧本需另做地理史料记录。

战国七雄、秦楚汉尚未制作。此轮解耦的是沙盘、数据和现有行动系统；分封、会盟、郡县、兵制等时代制度仍需扩展规则模块。正式战斗、存档、外交和人物成长尚未实现；切换剧本会重新演练。

## 悬浮与网格

原 pointermove 的限频分支错误地隐藏提示框，下一次采样再次显示，产生闪烁。现限频只跳过采样，保留提示；拖动、缩放、离开地图才收起。view-policy.js 是实际渲染与事件处理共同使用的策略。
网格仅在相机距离 80 内渐入，48 内达到正常近景清晰度；距离 80 及以上 opacity=0 且停止绘制网格对象。地盘颜色、边界与已选目标/路线仍保留。

## 验证

`node verify-chunqiu.mjs`、`node verify-chunqiu-geography.mjs` 检查春秋共用引擎；`node verify-scenarios.mjs` 分别在两个 SCENARIO 下验证城池可达、居民、AI 180 秒模拟、悬浮限频、近远网格以及错误配置。浏览器验收另含两剧本名录与近远景。悬浮高频输入为逻辑回归，不能代替所有设备上的实测。

## 大陆海岸背景（2026-09-24）

共用引擎接入 `geography/east-asia-land.js`，`engine/coast.js` 将 Natural Earth 外环经剧本映射生成一次性的 721² 岸距场。查询 O(1)，无逐帧遍历海岸。远景显示渤海、黄海、东海、南海；海底下沉避免海面与地形 z-fighting，沿岸浅色渐变使用单通道纹理。

中心 ±110 单位保留原地形顶点间距，只延展外围顶点；地形仍为 480² 网格。海面增加一个双三角形绘制对象，四个 DOM 海名；模拟格子和 AI 数量不变。两剧本的 cameraExtent、overviewTarget、farDistance 独立配置。大陆背景不等于全国可玩范围。原 /lab/ 不变。

海域接入水岸、地块通行和领土遮罩；春秋有 88 格判为不可走海域，三国可玩区仍在内陆。所有城市保持距海岸大于3单位且可达。`node verify-coast.mjs` 在两个 SCENARIO 下检查海名在水上、海格不可行、海南台湾与内陆采样为陆地、城市未淹没。地理来源及推测边界见 dist/geography/README.md。未做前台 GPU 帧率基准。

## 水面格线与城郊点缀（2026-09-24）

近景陆地、河流、湖泊和海域均显示六角格；远景仍隐藏。河湖格线抬至水面以上；海格由同一 SIZE 与世界原点的轴坐标着色器生成，覆盖背景海域而不创建新模拟格。显示网格不代表海域开放行军。海面着色器按陆海岸距裁切，避免海面从内陆河岸浅坡透出造成宽色带；河岸去掉宽沙色着色，并增加一个批量细线对象，海口处截断。

`settlements.js` 根据城池和有效地形确定性布置：春秋71处，三国61处。类型为聚落、市集、哨营、手工作坊。每城最多两处，找不到安全空地则跳过；避开水岸、坡地、其他城址、关隘、津渡和道路；树木也避开小据点。静态模型进入既有按材质合批流程，不增加AI或寻路格子。名称近景显示并参与标签避让；悬浮地格或左键地格可读其说明。它们暂为景观情报，不提供交易、征募、占领和产出。

晋地铸铜坊、齐地冶铸坊是考古启发，非精确遗址坐标或复原建筑；资料涉及不同时段，适用于跨时代春秋群英。其他名称为泛称、演练布置，不虚构特定部落历史。主题数据放在剧本 map.settlementThemes，通用引擎不写时代地名。

来源：
- 侯马晋国古都博物馆 https://www.jggdbwg.cn/ ：晋国都城及铸铜遗址背景。
- 中国社会科学网《临淄齐故城冶铸业考古》 https://www.cssn.cn/kgxc/kgxc_kghs/202207/t20220728_5430897.shtml ：齐故城冶铸遗存研究，年代跨越东周及秦汉，不用于断定本场景固定年代。

验证：`verify-settlements.mjs` 在两剧本下检查数量、确定性、地形与水岸、城市间距、可达性及不改变模拟；原河山、海岸、AI回归继续使用。浏览器检查水面近景格线、河岸、聚落提示与远景隐藏；未做前台GPU帧率基准。

## 春秋外围疆域补全（2026-09-24）

春秋原矩形有效区已改为稀疏六角格集合：候选格框 134×142，实际生成 13,011 格；38城、28势力、146人物、10名活动角色不变。三国剧本的矩形区域保持原状。`map.playableOutline` 是手工概化的外缘，结合已有海岸遮罩，表达秦的西部腹地、燕的北部、齐的沿海、楚吴越的南部及巴蜀外围；不是某年历史疆界复原。范围之外仍为地理背景，不将全国所有陆地都分给现有势力。

`engine/territory.js` 同时供地格生成、拾取边界和染色渐隐使用，消除旧 EXTENT 方形裁切；水岸遮罩同步按 activeExtent 扩展，避免旧遮罩夹取导致外缘河海染色错误。染色只覆盖真正生成的地块。新增陆地按既有山水规则参与寻路，海域和险山仍不可走；外围会存在自然隔绝区域，不承诺每个陆格可达。边缘八国均获得可从主角起点抵达的外围地块。国名重新放入扩大后的腹地，巴、蜀在远景也显示。

原 extent 保留用于原地形与背景过渡；activeExtent 用于景物覆盖和遮罩范围；cols/rows 是候选包围框，不再等于实际地格数。playableCellCount 仅作说明用，测试与实际生成数核对，新增剧本必须同步更新。邻接依赖 q,r 索引而非矩形数组偏移；不要恢复矩形格数假设。

验证：verify-territory.mjs 检查 13,011 格、轮廓外不生成、坐标唯一、邻接对称、城市归属、外围八国可达地块；其中旧 extent 外6,424格，4,645格可从起点抵达（其余含海、山或隔绝区域）。本机 Node 单次建图约68ms，仅CPU观察，不代表GPU帧率。全部城池、AI及聚落回归通过；关闭山口秦蜀路线54格改为128格绕行，关闭津渡秦晋仍不能通行。地形底网、AI数量不变；所有权和格线几何随实际格数增长，未宣称渲染成本不变。
# 2026-09-24：外围族群分布层

春秋群英新增义渠、白狄、山戎、越人诸部四片简化染色；远览显示名称及「族群分布」，随势力染色开关共同显示／隐藏。势力情报底部可展开说明。本次不添加城邑、人物、军队或可玩格子，仍为 13,011 格、28 国、38 城、146 人、10 个活动 AI。三国未配置该层。

数据位于 `map.culturalRegions`，每项包含 `id/name/region/color/label/outline/description`。`cultural-regions.js` 使用多边形有符号距离，裁去可玩区域和水面；在原地形顶点上预计算颜色权重，`overview.js` 在已有着色器中混色。没有新增绘制调用、地形三角形或逐帧多边形查询；增加一份每顶点 4 浮点的属性（当前约 3.7 MB）及四个远景 DOM 标签。不能把这种有限开销等同于已完成全国性能验证。

历史依据与设计边界：这是跨时期剧本中的分布意象，轮廓和标注经过压缩、外移以让出既有玩法区，**不能直接读取为经纬度、精确疆界或某年实际控制区**。未设城邑是本次简化，不表示这些族群没有聚落。尤其越人诸部不是一个统一国家，也不是汉代南越国。

- 义渠：甘肃地方史志《宁县志》建置沿革，参考陇东、泾水上游方向：[地方志 PDF](https://gsdfszw.org.cn/gsxz/qys_275/nx/201708/P020171123352248119569.pdf)。本次检索可读摘要，全文抓取超时，未据此拟定精确边界。
- 白狄：[白狄条目及参考文献](https://zh.wikipedia.org/wiki/%E7%99%BD%E7%8B%84)，采用早期陕北、晋西北的概括方位，不表示迁徙后的全部分布；该项目前为二手综述依据，后续可用考古材料细化。
- 山戎：[北京市文物局·山戎文化陈列馆](https://wwj.beijing.gov.cn/bjww/362771/362773/546619/index.html)，燕北、延庆春秋墓葬材料支持其相对方位。
- 越人诸部：[广东概况·建置沿革](https://www.gdhmo.gov.cn/gaikuang/content/post_68514.html)，支持春秋战国岭南与吴越楚的往来背景；图形为东南、岭南的概化。
- 概念边界：[王震中《夷夏互化融合说》](https://www.cssn.cn/dkzgxp/zgxp_zgshkx/2022nd1q/202208/t20220822_5474690.shtml)。不机械套用东夷／西戎／南蛮／北狄四个整齐象限。

验证：`node verify-cultural-regions.mjs` 检查所有可玩格、城市不受染色覆盖，名称落在本区陆地，水面不染色；以 `SCENARIO=sanguo` 再跑验证可选层。另跑 `verify-territory.mjs` 和两剧本 `verify-scenarios.mjs`，浏览器检查远览、图层开关及势力情报。

## 国家模拟 v1（2026-09-24）

共用引擎已接入国家内政、军事出征、迎战、野战、攻城易主、盟约与停战，详见 [国家模拟说明](STATE_SIMULATION.md)。此项取代本文旧段落中尚无外交／战斗的描述。主角本队仍为独立行军验证部队，暂不参与自动战斗；原 `/lab/` 不变，外围族群仍为显示层。


## 国家模拟 v2（2026-09-24）

运行层支持首领叛变、自立、流浪军筑城与实际工役队营建。城数与国家数不再固定；所有界面枚举应使用 campaign.cities／factions，新增设施按材质批处理并在重置时清理。规则、上限、默认种子和测试见 [国家模拟说明](STATE_SIMULATION.md)。
