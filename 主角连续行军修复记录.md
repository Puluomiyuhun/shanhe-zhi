# 主角连续行军修复

原实现用两套时间：按下一格地形成本 × 480 ms 触发逻辑结算，然后固定用 330 ms 播放动画。平原一格 480 ms，林地 864 ms，山地 1,152 ms，导致动画播完后站着等待；官道 312 ms 反而可能在旧动画未结束时覆盖动画。

改为统一连续时间：每帧积累本段进度，按地形速度插值；到达格心才结算占领、补给、士气和剩余路线，未用完的帧时间继续下一段。暂停保留段内进度，继续不会重新计时；改道从当前可见位置接上新路线，取消保持当前位置。「推进一刻」仍明确完成一格。

已有领土内移动不再重建整张地盘染色与边界；仅真正改变归属时更新，减少逐格 CPU 工作。

验证：`node verify-march-motion.mjs` 沿襄阳—新野的 18 格路线采样 398 帧，行军期间无静止帧；覆盖暂停稳定、不同帧时间切分结果一致、改道无瞬移、取消静止、手动单步。`node verify-order-route.mjs` 回归通过。浏览器验证行军抵达和控制台无错误。
