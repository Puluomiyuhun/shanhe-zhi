# 城池密度与性能记录

## 本轮规模

地图仍为 104 × 112 六角格（11,648 格），可玩边界不变。城池由 14 增到 35，势力由 5 增到 9。新增马腾、刘焉、董卓、刘繇，填补武都、巴蜀、北缘、东南方向。现有六名 AI 保持运行，未以增加大量武将混淆城池扩充的负载。

城名取历史地名，位置和归属为压缩、混合的演练配置，不是同一年份历史疆域复原。外围远山仍是背景，新增据点覆盖可玩地图四周。

## 显示和性能处理

- 延续静态城池按材质合批：更多建筑没有按城市数成倍增加绘制调用。
- 预计算城市山口走廊，先用包围范围排除无关城市，再计算精确距离；不削减地形网格分辨率。
- 城市侧栏缓存固定地形上的寻路结果和城郊地块；换城、主角移动或重置后更新，驻城人物和归属仍动态刷新。
- 拥挤城市名称收成阵营小旗，保留可点击按钮、辅助名称；悬停或键盘焦点展开。选中城和主角驻城优先保留名称。
- 九势力图例换行，避免挤出界面。
- 运行状态使用最近 600 帧样本，显示 P95、超过 33.4 ms 的慢帧数量、建图耗时和绘制规模。后台恢复时重新采样，不把后台暂停当作慢帧。

## 实测（当前本机内置浏览器，1280 × 720，默认画质）

| 场景 | 扩充前 | 扩充后 |
|---|---:|---:|
| 远景帧率 | 60 FPS | 60 FPS |
| 远景 P95 | 16.8 ms | 16.8 ms |
| 远景绘制调用 | 16 | 16 |
| 远景每帧三角面 | 约 203.8 万 | 约 205.3 万 |
| 近景、六 AI 运行 | 未单独取同口径样本 | 60 FPS，P95 16.8 ms |

扩充后远景最近 600 帧，慢帧 0；建图约 3.6 秒。帧率受当前刷新率上限限制，不能由 60 FPS 推算 GPU 剩余空间。此结果不代表低配设备或大规模部队战斗测试。

Node 同进程对照（预热后）：

| CPU 操作 | 14 城 | 35 城＋优化 |
|---|---:|---:|
| 逻辑地图构造（3 次） | 15 / 17 / 17 ms | 11 / 10 / 12 ms |
| 10 万次地形高度采样 | 84 ms | 62 ms |
| 襄阳至全部城市逐一寻路合计 | 52 ms（14 条） | 209 ms（35 条） |
| 以上路线最慢一次 | 18.0 ms | 18.3 ms |

全城寻路总耗时增加主要来自查询数量。单次远距寻路约一个帧预算，未来大量武将同时重新决策时应分帧或放入 Worker；当前六名 AI 错开发令，未实施批量并发寻路压测。

## 复现与验证

运行 `node verify-factions.mjs`、`node verify-expansion.mjs`、`node verify-actors.mjs`、`node verify-density.mjs`。覆盖全部城市可达、位置在界内、势力有效、渡桥补给以及五分钟 AI 行动循环。性能脚本报告当前主机观测值，不设跨设备固定毫秒门槛。

浏览器点「远览」或「近观」，点击「推进」运行 AI；在「操作说明 → 样区说明与运行状态」查看最近采样。开说明会暂停世界，因此 AI 场景应先运行、再查看记录。
