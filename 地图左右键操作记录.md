# 地图左右键操作

- 左键地块或城牌查看情报、路线成本，不下军令。
- 右键地块或城牌直接移动至目标，自动开始推进，不再需要确认或额外点推进。新目标替换旧路线。
- 河流、险山、不可达目标或士气耗尽时拒绝新命令并提示；不覆盖原军令。
- 右键当前所在格撤销本队剩余路线，原地待命。
- 游戏文档统一屏蔽原生 contextmenu，覆盖地图、城牌、头像和侧栏。
- 地图左拖平移，右拖旋转；以按下位置累计 5px 位移区分点击和拖动，防止缓慢拖动误判点击。取消指针和失去捕获后清理状态。
- 侧栏「移动至此」、前往新野、人物返回襄阳统一使用直接军令入口；空格暂停或继续。

## 验证

浏览器实测：左键新野只显示情报且推进保持暂停；右键新野立即显示行军中和 18 格路径；左键地块仅显示情报，右键同一地块立即开始 3 格行军；右键河流显示不可通行反馈，无网页菜单；控制台无错误。拖动阈值、取消捕获及按键匹配按代码检查。
