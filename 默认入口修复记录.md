# 默认入口修复

旧站点根目录 index.html 仍承载最初二维地图，三维版本位于 lab/，导致打开根地址误以为代码未更新。

现在 HTTP 根地址与 index.html 自动进入 lab/；旧二维页面原样保存到 classic.html，三维页「原版」及 WebGL 降级链接均指向 classic.html。直接以 file 协议打开时显示启动 HTTP 服务器的说明。GitHub README 更新为 35 城、9 势力、右键移动、连续行军及正确入口。

浏览器从 localhost 根地址打开，已确认自动进入 lab/ 并显示最新版界面。
